version: 2
seed: 42

generator:
  type: baseline
  n_samples: 1000

guard:
  enabled: false
  mode: detect

scenario:
  enabled: false

audit:
  weights:
    quality: 0.4
    utility: 0.3
    privacy: 0.3

